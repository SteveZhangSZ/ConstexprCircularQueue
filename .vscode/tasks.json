{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "clang++ build active file",
			"command": "/Users/stevezhang/Desktop/miniconda3/bin/clang++",
			"args": [
				"-g",
				"-std=c++1z",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
		}
	]
}